# Copy this file to keystore.properties and keep the real file private.
# Paths are resolved from the android/ directory. Forward slashes work on Windows.
storeFile=../private-signing/colorquest-upload.jks
storePassword=REPLACE_WITH_PRIVATE_PASSWORD
keyAlias=colorquest-upload
keyPassword=REPLACE_WITH_PRIVATE_PASSWORD
